server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # LA RIGA MAGICA: Se non trovi il file, vai su index.html
        try_files $uri $uri/ /index.html;
    }
}